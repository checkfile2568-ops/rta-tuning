# Copy to .env on each private worker host. Never commit the real secret.
# WORKER_ID must be different on every computer, for example mms-home-01 and
# mms-office-01.  Jobs and media stay in Supabase, so any online worker can
# safely receive the next queued job.
SUPABASE_URL=https://hwzadnpaxiacucvjxmor.supabase.co
SUPABASE_SERVICE_ROLE_KEY=PASTE_ON_PRIVATE_HOST_ONLY
WORKER_ID=mms-home-01
WORKER_LABEL=บ้าน
POLL_SECONDS=2
HEARTBEAT_SECONDS=15
